services:

  # خدمة الـ PHP (البرنامج بتاعك)
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: healthify-backend-api
    restart: unless-stopped
    volumes:
      - ./:/var/www
    depends_on:
      - db
    networks:
      - healthify-net

  # خدمة الـ Nginx (السيرفر اللي هيستقبل الطلبات)
  web:
    image: nginx:alpine
    container_name: healthify-nginx
    restart: unless-stopped
    ports:
      - "8000:80" # هتفتح الـ API من localhost:8000
    volumes:
      - ./:/var/www
      - ./docker-config/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - healthify-net

  # خدمة الـ MySQL (الداتابيز)
  db:
    image: mysql:8.0
    container_name: healthify-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: healthify
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: healthify_user
      MYSQL_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - healthify-net

networks:
  healthify-net:
    driver: bridge

volumes:
  dbdata: